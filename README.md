# Job Notifier

Goal: build an MVP that scrapes job portals and notifies users when new matching job postings are discovered.

Structure:
- /backend  - Express API, auth, models, routes
- /worker   - scrapers, queues, worker processes
- /frontend - React app (Vite or CRA)

See docs/ for more details.
